extends base
include mixins/video

block content
    div 
        if searchResult.length
            each video in searchResult
                +video(video)
        else
            span No Video Found