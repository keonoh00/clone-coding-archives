mixin video(video)
    div
        a(href=`/video/${video.id}`)=video.title
        ul
            li #{video.rating}/5
            li #{video.comments} comments
            li created #{video.created}
            li #{video.views} views