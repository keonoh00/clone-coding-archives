mixin video(video)
    div
        h4 
            a(href=`/video/${video.id}`)=video.title
        p=video.description
        div
            each tag in video.hashtags
                small #{tag}&emsp;
        small=video.createdAt
        hr