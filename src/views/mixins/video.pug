mixin video(video)
    div
        h4 
            a(href=`/video/${video.id}`)=video.title
        p=video.description
        each tag in video.hashtags
            li=tag
        small=video.createdAt
        hr